Commands for Login and register pages:
- composer require laravel/ui
- php artisan ui bootstrap --auth
- npm install
- npm run dev

- php artisan serve
- php artisan migrate

1.to register as admin you must first specify admin name , email , password.
In the database table is_admin, the standard must be set to 1 for admin and 
0 for other users
see the logic at (Middleware/is_Admin file) and (Auth/LoginController.php)
/home : for users dashborde.
/admin/home : for admin dashbord.

Commands for crud:
- php artisan make:migration create_contacts_table 
- php artisan migrate
- php artisan make:controller ContactController --resource
- php artisan make:model Contact

copy my code in this files

for uploade images , you can finde the images in public/images folder
for Progress is same and task2